# Optional. Leave unset in development — Vite proxies /api to the backend
# (see the `server.proxy` block in vite.config.js), which avoids CORS entirely.
# Set this for a production build that talks to an API on another origin.
# VITE_API_URL=https://api.your-company.com/api
