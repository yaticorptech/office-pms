{
  "name": "office-pms",
  "version": "1.0.0",
  "private": true,
  "description": "Workly — Office Project & Task Management System (MERN)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm --prefix server ci && npm --prefix client ci",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start",
    "test": "npm --prefix server test",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "seed": "npm --prefix server run seed",
    "seed:fresh": "npm --prefix server run seed:fresh",
    "postinstall": "npm --prefix server install && npm --prefix client install"
  }
}
